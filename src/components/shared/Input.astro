---
export interface Props {
  className?: string
  type: any
  label: string
  error: string
  name: string
  placeholder: string
}

const { name, label, placeholder, type, className, error } = Astro.props
---

<div class='flex flex-col'>
  <label class='font-semibold text-gray-900 dark:text-white' for={name}
    >{label}</label
  >
  <input
    id={name}
    name={name}
    type={type}
    placeholder={placeholder}
    class='transition-all w-full p-4 border rounded-lg bg-transparent backdrop-blur-sm outline-none outline-offset-0 focus:outline-1 focus:outline-blue-500 focus:border-blue-700 appearance-none'
  />
  {error && <p class='text-sm text-end'>{error}</p>}
</div>
