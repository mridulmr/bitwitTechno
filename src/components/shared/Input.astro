---
export interface Props {
  className?: string;
  variant: "primary" | "secondary";
  type: any;
  label: string;
  error: string;
  name: string;
  placeholder: string;
}

const { name, label, placeholder, type, variant, className, error } =
  Astro.props;
---

<div class="flex flex-col">
  <label class="font-bold text-blue-950 dark:text-white" for={name}>{label}</label>
  <input
    id={name}
    name={name}
    type={type}
    placeholder={placeholder}
    class="w-full p-4 border rounded-lg bg-transparent backdrop-blur-sm"
  />
  {error && <p class="text-sm text-end">{error}</p>}
</div>
